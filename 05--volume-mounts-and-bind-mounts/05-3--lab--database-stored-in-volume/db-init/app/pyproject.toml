[project]
name = "db-init"
version = "0.1.0"
requires-python = ">=3.12,<4.0"
dependencies = [
    "alembic>=1.19.2,<2.0.0",
    "sqlmodel>=0.0.42,<0.1.0"
]

[build-system]
requires = ["poetry-core>=2.4.3,<3.0.0"]
build-backend = "poetry.core.masonry.api"
